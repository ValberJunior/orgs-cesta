import { StyleSheet } from "react-native";

export default StyleSheet.create({
    title:{
        color: "#464646",
        marginTop: 32,
        marginBottom: 8,
        fontSize: 20,
        lineHeight: 32,
    }
});